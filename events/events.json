---
---
[
{% for post in site.posts reversed %}
	{
		"title": "{{ post.title }}",
		"start": "{{ post.date | date_to_xmlschema }}",
		"url": "{{ site.baseurl }}{{ post.url }}",
		"className": {% for t in post.types limit:1 %}"{{t | slugify }}"{% endfor %}
	}{% if forloop.last %}{% else %},{% endif %}
{% endfor %}
]
